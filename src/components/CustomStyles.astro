---
/* Use Google Fonts to match legacy site */
/* Note: can be self-hosted later for performance/privacy */
---

<style is:inline>
  @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700;800&family=Lora:wght@400;500;600;700&display=swap');
  :root {
    /* Typography (Option 1) */
    --aw-font-sans: 'Lora', ui-serif, Georgia, Cambria, 'Times New Roman', Times, serif; /* Body */
    --aw-font-serif: 'Lora', ui-serif, Georgia, Cambria, 'Times New Roman', Times, serif;
    --aw-font-heading: 'Montserrat', ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, Ubuntu, Cantarell, 'Noto Sans', 'Helvetica Neue', Arial;

    /* Palette (Option 1: Corporate) */
    --aw-color-primary: #1A237E; /* Deep navy */
    --aw-color-secondary: #FFFFFF; /* Clean white (avoid for primary hover text) */
    --aw-color-accent: #FFCA28; /* Ochre / gold */

    --aw-color-text-heading: #0A0E1A;
    --aw-color-text-default: #1F2430;
    --aw-color-text-muted: rgb(31 36 48 / 66%);
    --aw-color-bg-page: #FFFFFF;

    ::selection {
      background-color: #FFEEA8;
    }
  }
</style>
